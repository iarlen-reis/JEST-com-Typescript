describe("TESTANDO ALGUMA COISA", () => {
  it("should return one", () => {
    const number = 1;

    expect(number).toBe(1);
  });

  test("should return Iarlen", () => {
    const name = "Iarlen";

    expect(name).toBe("Iarlen");
  });
});
